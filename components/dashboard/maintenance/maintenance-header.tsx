'use client'

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export function MaintenanceHeader() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Maintenance & Communications Hub</CardTitle>
        <CardDescription>Manage maintenance requests, communications, and automated reminders</CardDescription>
      </CardHeader>
    </Card>
  )
}
